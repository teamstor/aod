Gör så att tiles har string IDs istället för byte IDs
Gör om map formatet till JSON och skapa en migrator för den gamla versionen
{
	"name": "Untitled",
	"width": 60,
	"height": 60
	"layers": {
		"terrain": [
			"", "", "grass", "grass" // "" ska vara lika med ID 0 för optimering
		]
	}
}
Gör så att map formatet inne i spelet bygger på en "palette" där man kan mappa IDs och byte/short?
Map formatet ingame kanske ska ha så små värden i en bit array som möjligt (om man bara har water och grass så är det 1-bit, etc)
(TODO: ska ha chunks eller inte?)
Skapa funktioner så att man simplet kan overridea tile attribut som t.ex. new Tile(...).
	OverrideUsesTransition(true /* simpel, skapar en delegate/lambda/whatever */).
	OverrideUsesTransition(newFunction /* med funktion */)
(kanske gå över till ett component system? t.ex. [UsableIn(Map.Environment.Forest, Map.Environment.Inside)]
Gör så att jag skapar kategorier manuellt för map editorn och den använder Tile.Draw (när man hoverar kommer namn och en vit outline)
